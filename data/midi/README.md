# Training MIDI

Place your `.mid` or `.midi` training files in this folder, then run:

```bash
python preprocess.py
python train.py
```

Use a reasonably consistent collection for better musical results, and only use files you have permission to use for training.
